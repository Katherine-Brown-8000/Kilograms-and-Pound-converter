w = float(input("Weight: "))
metric = str(input("Kilograms 'K' or pounds 'L': "))

if metric == 'K':
    kc = w*2.20
    print(f"Weight in pounds is {kc}")
if metric == 'L':
    lc = w/2.20
    print(f"Weight in kilograms is {lc}")
